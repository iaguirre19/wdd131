// Main JS entry for the Final Project
// You can split this later into multiple files inside the js/ folder if needed.

document.addEventListener('DOMContentLoaded', () => {
  // Init code here
  console.log('Final Project JS loaded');
});

